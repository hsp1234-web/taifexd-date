# -*- coding: utf-8 -*-
"""
範例測試模組

這個檔案包含一些基礎的 Pytest 測試案例，
用以驗證測試環境是否設定正確。
"""

def test_範例斷言():
    # 這是一個簡單的範例測試
    # 它斷言 True 應該為 True，這總會通過
    # 目的是確保 Pytest 可以找到並執行測試
    assert True, "基礎斷言失敗，請檢查 Pytest 設定"

def test_加法():
    # 測試一個簡單的加法運算
    a = 1
    b = 2
    # 斷言 a + b 的結果是否等於 3
    assert a + b == 3, f"{a} + {b} 應為 3，但結果不是"

# 你可以在這裡加入更多測試案例
